node_modules/
dist/
vendor/
dados/
*.db
*.db-journal
.gestor-trafego-test/
# T5 legacy: runtime Node 18 + tarball vendorados fora do controle de versão
# (o pin versão+URL+SHA vive em src/server/runtime/legacy.ts, o binário não).
dist/legacy/runtime/
vendor/node-legacy/
# legado binário do Bun (<1.0); o lock atual bun.lock É versionado de propósito
bun.lockb
